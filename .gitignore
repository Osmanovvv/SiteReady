# Dependencies (reinstall with: cd web && npm install)
node_modules/
web/node_modules/

# Local env overrides
*.local
.env.local
web/.env.local

# Logs & OS cruft
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Machine-local tooling state
.claude/*.lock

# Audit history (local data, PLAN-v2 §2)
data/

# Note: web/dist IS tracked on purpose — the engine serves it, so `node server.js`
# runs the app with no build step. Rebuild with: cd web && npm run build
